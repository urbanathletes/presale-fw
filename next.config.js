/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    unoptimized: true,
  },
  // experimental: {
  //   forceSwcTransforms: true,
  // },
}

module.exports = nextConfig
